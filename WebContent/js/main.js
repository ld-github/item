var URLS = {
    GET_INFO : contextPath + '/user/getInfo',
}

$(function() {

});

$(function() {
    var app = new Vue({
        el : '#app',
        data : {
            message : 'hello, vue'
        }
    });
});